export interface paymentPlanId {
  productId: string;
  quanityProduct: Number;
  access_token: any;
}
export interface IdLinkPath {
  detailLink: string;
  title: string;
}
export interface Field {
  name: string;
}
export interface DetailLoadingProps {
  loading: boolean;
}
